# Arquivos baixados (podem chegar a dezenas de GB)
data/pdfs/

# Controle de progresso (gerado automaticamente pelo script)
data/downloaded.txt
data/failed.txt

# Python
__pycache__/
*.py[cod]
*.pyo
.venv/
venv/
env/
.env

# IDEs
.vscode/
.idea/
*.swp
*.swo

# macOS
.DS_Store
._*

# Windows
Thumbs.db
desktop.ini

# Resultados individuais do teste de parsing (gerados automaticamente)
data/test_sample/resultados/

# Varredura de PDFs escaneados (gerado automaticamente)
data/scan_report.json
data/scan_progress.txt

# Corpus gerado pelo parser (pode chegar a vários GB)
data/corpus/

# Controle e saídas do parser
data/parsed.txt
data/parse_errors.json
data/parse_summary.json
data/skipped_scanned.json
data/missing_files.json

# Saídas do OCR (geradas automaticamente)
data/ocr_errors.json
data/ocr_summary.json
data/ocr_parsed.txt

# Logs
*.log
.claude/